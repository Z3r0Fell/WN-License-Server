# WatchNexus licensing server - lean runtime requirements.
# Only what the application actually imports. Versions chosen to be
# available on PyPI and mutually compatible. This file is hand-curated to
# keep Docker builds reproducible; do not auto-regenerate with `pip freeze`
# from a dev container or you'll re-introduce hallucinated future versions.

# --- web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.0
starlette==0.38.6
python-multipart==0.0.12

# --- data validation / typing ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0
typing-extensions>=4.12.0

# --- mongodb ---
motor==3.6.0
pymongo==4.9.2

# --- auth / crypto ---
PyJWT==2.9.0
bcrypt==4.2.1
cryptography==44.0.1

# --- env / config ---
python-dotenv==1.0.1

# --- outbound integrations (lazy-imported in code; safe to install) ---
sendgrid==6.11.0
requests==2.32.3

# --- 2FA ---
pyotp==2.9.0
qrcode[pil]==7.4.2
